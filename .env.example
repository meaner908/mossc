MOSSC_PORT=3000
OPENCLAW_STATE_DIR_HOST=C:/Users/UserName/.openclaw
# Optional: set a password to protect the app when deployed on a public network.
# When set, a login page will be shown before accessing MossC.
# Leave empty (or remove) to disable password protection.
MOSSC_PASSWORD=